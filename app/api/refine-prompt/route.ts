import { NextRequest, NextResponse } from 'next/server'
const GEMINI_URL='https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent'
export async function POST(req: NextRequest){try{const{currentPrompt,instruction}=await req.json();if(!currentPrompt?.trim()||!instruction?.trim())return NextResponse.json({error:'Faltan parametros'},{status:400});const apiKey=process.env.GEMINI_API_KEY;if(!apiKey)return NextResponse.json({error:'GEMINI_API_KEY no configurada'},{status:500});const prompt=`Eres un experto en ingenieria de prompts. Refina y mejora el prompt segun las instrucciones del usuario. Manten las variables dinamicas {{nombre}} exactamente igual. Preserva el idioma original. Solo devuelve el prompt refinado, sin explicaciones ni comentarios. No anadas markdown. PROMPT ACTUAL: ${currentPrompt} INSTRUCCION: ${instruction} Devuelve el prompt refinado:`;const res=await fetch(`${GEMINI_URL}?key=${apiKey}`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({contents:[{parts:[{text:prompt}]}],generationConfig:{temperature:0.7,maxOutputTokens:1024}})});const data=await res.json();if(!res.ok)return NextResponse.json({error:data.error?.message||'Error Gemini'},{status:500});const refined=data.candidates?.[0]?.content?.parts?.[0]?.text?.trim();if(!refined)return NextResponse.json({error:'Respuesta vacia de IA'},{status:500});return NextResponse.json({refined})}catch(err){return NextResponse.json({error:'Error interno: '+String(err)},{status:500})}}